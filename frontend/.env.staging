# Base path of the CareerLinkAI API (FULLPLAN §19). Must include the /api/v1 prefix.
#
# Points at the staging Worker (backend/wrangler.toml [env.staging]), whose FRONTEND_URL
# is set to https://careerlinkai-staging.pages.dev — the only origin its CORS admits.
VITE_API_BASE_URL=https://careerlinkai-staging.cejascarldindo.workers.dev/api/v1
